# Налаштування Google OAuth в Supabase

## Крок 1: Налаштування Google OAuth в Supabase Dashboard

1. Зайдіть на [supabase.com](https://supabase.com) та відкрийте ваш проект
2. Перейдіть до **Authentication** → **Providers**
3. Знайдіть **Google** та натисніть **Enable**
4. Заповніть наступні поля:
   - **Client ID (for OAuth)**
   - **Client Secret (for OAuth)**

### Як отримати Google OAuth credentials:

1. Зайдіть на [Google Cloud Console](https://console.cloud.google.com/)
2. Створіть новий проект або виберіть існуючий
3. Перейдіть до **APIs & Services** → **Credentials**
4. Натисніть **Create Credentials** → **OAuth client ID**
5. Якщо потрібно, налаштуйте OAuth consent screen:
   - Application type: **Web application**
   - Name: **Wallet Dashboard** (або ваша назва)
   - Authorized redirect URIs: 
     - `https://YOUR_PROJECT_ID.supabase.co/auth/v1/callback`
     - `http://localhost:5173` (для локальної розробки)
6. Скопіюйте **Client ID** та **Client Secret**
7. Вставте їх в Supabase Dashboard

### Redirect URI для GitHub Pages:

Якщо ви задеплоїли на GitHub Pages, додайте також:
```
https://YOUR_USERNAME.github.io/YOUR_REPO/auth/v1/callback
```

Або краще використовувати домен Supabase:
```
https://YOUR_PROJECT_ID.supabase.co/auth/v1/callback
```

## Крок 2: Налаштування Redirect URLs в Supabase

1. Перейдіть до **Authentication** → **URL Configuration**
2. Додайте **Redirect URLs**:
   - `http://localhost:5173` (для локальної розробки)
   - `http://localhost:5173/*` (для всіх локальних шляхів)
   - `https://YOUR_USERNAME.github.io` (для GitHub Pages)
   - `https://YOUR_USERNAME.github.io/*` (для всіх шляхів на GitHub Pages)
   - `https://YOUR_USERNAME.github.io/YOUR_REPO_NAME` (якщо репозиторій не на root)
   - `https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/*` (якщо репозиторій не на root)
   - `https://YOUR_CUSTOM_DOMAIN.com` (якщо є)
   
**ВАЖЛИВО:** Додайте всі варіанти з `/*` в кінці, щоб Supabase приймав redirect на будь-який шлях вашего сайту!

## Крок 3: Row Level Security (RLS) - ВАЖЛИВО! 🔒

**Відповідь на питання:** Ні, дані не переплутаються! Навіть якщо всі транзакції та карти зберігаються в одних таблицях, RLS автоматично фільтрує дані по `user_id`. Кожен користувач бачить **тільки свої** дані.

### Як це працює:

1. **Всі дані в одних таблицях** - це нормально та правильно
2. **RLS автоматично фільтрує** - коли користувач робить запит, Supabase автоматично додає `WHERE user_id = auth.uid()`
3. **Користувач НЕ може** побачити або змінити дані інших користувачів
4. **Це стандартний підхід** для multi-tenant додатків

### Швидке налаштування через SQL Editor:

1. Перейдіть до **SQL Editor** в Supabase Dashboard
2. Створіть новий запит
3. Скопіюйте весь скрипт з файлу `SUPABASE_RLS_SETUP.sql`
4. Виконайте скрипт (Run)

**АБО** налаштуйте вручну:

### Для таблиці `transactions`:

1. Перейдіть до **Table Editor** → `transactions`
2. Натисніть **Enable RLS**
3. Створіть політики через SQL Editor:

```sql
-- Політика: Користувачі можуть бачити тільки свої транзакції
CREATE POLICY "Users can view own transactions"
ON transactions FOR SELECT
USING (auth.uid() = user_id);

-- Політика: Користувачі можуть створювати тільки свої транзакції
CREATE POLICY "Users can insert own transactions"
ON transactions FOR INSERT
WITH CHECK (auth.uid() = user_id);

-- Політика: Користувачі можуть оновлювати тільки свої транзакції
CREATE POLICY "Users can update own transactions"
ON transactions FOR UPDATE
USING (auth.uid() = user_id);

-- Політика: Користувачі можуть видаляти тільки свої транзакції
CREATE POLICY "Users can delete own transactions"
ON transactions FOR DELETE
USING (auth.uid() = user_id);
```

### Для таблиці `cards`:

```sql
-- Політика: Користувачі можуть бачити тільки свої картки
CREATE POLICY "Users can view own cards"
ON cards FOR SELECT
USING (auth.uid() = user_id);

-- Політика: Користувачі можуть створювати тільки свої картки
CREATE POLICY "Users can insert own cards"
ON cards FOR INSERT
WITH CHECK (auth.uid() = user_id);

-- Політика: Користувачі можуть оновлювати тільки свої картки
CREATE POLICY "Users can update own cards"
ON cards FOR UPDATE
USING (auth.uid() = user_id);

-- Політика: Користувачі можуть видаляти тільки свої картки
CREATE POLICY "Users can delete own cards"
ON cards FOR DELETE
USING (auth.uid() = user_id);
```

**ВАЖЛИВО:** Переконайтесь, що в таблицях є колонка `user_id` типу `uuid` з посиланням на `auth.users(id)`.

## Крок 4: Додавання user_id до існуючих таблиць

**Якщо у вас ще немає колонки `user_id`, додайте її:**

```sql
-- Для transactions
ALTER TABLE transactions 
ADD COLUMN IF NOT EXISTS user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE;

-- Для cards
ALTER TABLE cards 
ADD COLUMN IF NOT EXISTS user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE;

-- Створення індексів для швидкого пошуку
CREATE INDEX IF NOT EXISTS idx_transactions_user_id ON transactions(user_id);
CREATE INDEX IF NOT EXISTS idx_cards_user_id ON cards(user_id);
```

**Примітка:** Якщо у вас вже є існуючі дані без `user_id`, потрібно буде:
1. Визначити, які користувачі мають які дані
2. Вручну призначити `user_id` для існуючих записів
3. АБО видалити старі дані і почати заново

## ⚠️ КРИТИЧНО: Налаштування RLS

**ВИКОНАЙТЕ ЦЕ ОБОВ'ЯЗКОВО!**

Детальні інструкції в [SECURITY_IMPORTANT.md](./SECURITY_IMPORTANT.md)

**Коротко:**
1. Відкрийте Supabase Dashboard → SQL Editor
2. Скопіюйте весь вміст з `SUPABASE_RLS_SETUP.sql`
3. Виконайте скрипт (Run)
4. Перевірте, що RLS увімкнено для обох таблиць

**Без RLS ваш додаток НЕ БЕЗПЕЧНИЙ!**

## Перевірка

1. Запустіть додаток локально
2. Спробуйте увійти через Google
3. Перевірте, що після входу відображається основний інтерфейс
4. Перевірте в Supabase Dashboard → Authentication → Users, що користувач створився

## Troubleshooting

### Помилка "redirect_uri_mismatch"
- Перевірте, що redirect URI в Google Cloud Console точно відповідає тому, що вказано в Supabase
- Для локальної розробки використовуйте `http://localhost:5173`
- Для production використовуйте домен Supabase callback

### Дані не відображаються після входу
- Перевірте, що RLS налаштований правильно
- Перевірте, що `user_id` додається до нових записів
- Перевірте консоль браузера на помилки
- Переконайтесь, що виконали SQL скрипт з `SUPABASE_RLS_SETUP.sql`

### Дані користувачів переплутались
- Перевірте, що RLS увімкнений для обох таблиць (`transactions` та `cards`)
- Перевірте, що політики створені правильно
- Перевірте, що `user_id` автоматично додається при створенні нових записів
- **Якщо проблеми залишаються:** Виконайте повний скрипт з `SUPABASE_RLS_SETUP.sql`

## Важливо розуміти:

✅ **Правильно:** Всі дані в одних таблицях + RLS автоматично фільтрує  
❌ **Неправильно:** Окремі таблиці для кожного користувача (занадто складно)

**RLS = автоматичний фільтр безпеки.** Навіть якщо в базі мільйони записів від різних користувачів, кожен користувач бачить тільки свої дані!

